// Mirrors NotificationsScreen in lib/screens/notifications_screen.dart —
// genuinely a placeholder in the Flutter source ("This is the real
// Notifications page!"), so no state is needed.
export function useNotificationsViewModel() {
  return { message: 'This is the real Notifications page!' };
}
